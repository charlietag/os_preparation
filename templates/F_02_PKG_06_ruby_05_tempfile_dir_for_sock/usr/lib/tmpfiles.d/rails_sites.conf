# ===========================================
# To mkdir /var/run/rails_sites when reboot
# (/var/run is a tmpfs in memory, gone everytime reboot)
# ===========================================
d /run/rails_sites 755 ${current_user} ${current_user}
